/target
**/*.rs.bk
Cargo.lock.bak
.vscode/
.idea/
*.swp
.DS_Store

# Local sqlite scratch (the real DB lives at ~/.hive/hive.db, not in-repo)
*.db
*.db-shm
*.db-wal

# Leptos build artifacts
/site
/dist

# Local model cache for hive-embed
/models
