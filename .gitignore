.vscode/
.idea/
*.swp
.DS_Store

# Rust
target/
**/target/

# Node / Solid app
node_modules/
**/node_modules/
packages/*/dist/
data/*.db
data/*.db-shm
data/*.db-wal
*.tsbuildinfo

# Claude Desktop bundle build output (scripts/build-mcpb.sh)
/dist/

# agent worktrees (managed by the Agent tool)
.claude/worktrees/

# `/node/` used to be ignored here as a stale duplicate of the pre-teardown
# Node layout. That directory is long gone and `node/` is now the hive-node
# crate (PLAN-v2.1 PR 4.6) — the rule would silently hide a whole crate.
hive.local.env

# flatpak local build artifacts
packaging/flatpak/build-dir/
.flatpak-builder/
