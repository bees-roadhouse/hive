# Force a clean install inside the image; never ship host artifacts.
**/node_modules
**/dist
**/.cache
.git
.github
**/.claude
data
*.log
.env
.env.*
# Rust build artifacts + local databases never enter the build context.
target
**/target
*.db
*.db-wal
*.db-shm
