# Demo artefacts. The PKI is a throwaway CA regenerated by make-pki.sh on every
# run; the taps are packet captures of a local session. None of it belongs in
# the tree, and the keys least of all.
pki/
*.bin
*.log
