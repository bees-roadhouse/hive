// Journal append/list/get + anchors + bracket-token refs + visibleJournal ACL.
// Owned by the core-stores workstream — port store.ts `journal`, `anchors`,
// `journalWriters`, `visibleJournal`, `visibleEntryIds` exactly.
