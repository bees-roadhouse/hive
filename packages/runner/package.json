{
  "name": "@hive/runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Drives hosted Claude Code sessions for hive: claims provisioning workspaces, runs Claude Code via the Agent SDK in an isolated sandbox, and streams the full transcript back to the hive API.",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.195"
  }
}
