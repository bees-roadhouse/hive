{
  "name": "hive-memory",
  "displayName": "Hive Memory",
  "version": "0.7.0",
  "description": "NOT WORKING — do not install. It was built for the removed hive-bridge stdio transport against a local hive data directory; there is no bridge crate and no local store, so .mcp.json fails to spawn and hooks-handlers/*.mjs soft-fail. Kept as the porting target for an HTTP rewrite against POST /mcp. README has the path that works today.",
  "author": {
    "name": "Bee's Roadhouse",
    "url": "https://github.com/bees-roadhouse/hive"
  },
  "homepage": "https://github.com/bees-roadhouse/hive",
  "repository": "https://github.com/bees-roadhouse/hive",
  "license": "MIT",
  "keywords": ["hive", "memory", "journal", "recall", "identity", "mcp", "local-first"],
  "hooks": "./hooks/hooks.json",
  "mcpServers": "./.mcp.json"
}
