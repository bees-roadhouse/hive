//! `hive` ... shared-state CLI.
//!
//! Drop-in replacement for `python ~/.hive/hive.py`. Subcommand grammar and
//! output shape match the python verbatim; this is enforced by snapshot
//! tests under `tests/`.

fn main() {
    eprintln!("hive: scaffolding ... CLI parity work tracked in task #3");
    std::process::exit(2);
}
