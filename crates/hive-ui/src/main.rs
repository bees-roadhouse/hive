//! `hive-ui` ... leptos SSR + WASM hydration. Replaces the python+svelte UI.
//!
//! Scaffolding only. Pages and hydration land in task #5.

#[tokio::main]
async fn main() {
    eprintln!("hive-ui: scaffolding ... leptos pages land in task #5");
    std::process::exit(2);
}
