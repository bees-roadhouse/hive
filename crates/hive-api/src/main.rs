//! `hive-api` ... axum HTTP API. Routes mirror the CLI grammar.
//!
//! Scaffolding only. Real routes land in task #4.

#[tokio::main]
async fn main() {
    eprintln!("hive-api: scaffolding ... routes land in task #4");
    std::process::exit(2);
}
